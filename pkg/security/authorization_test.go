package security

import (
	"testing"
)

func TestAuthorize(t *testing.T) {
	// router := gin.Default()

	// router.GET("/*nodes", Handler()

	// w := httptest.NewRecorder()
	// r, _ := http.NewRequest("GET", test.url, nil)

	// router.ServeHTTP(w, r)
}
